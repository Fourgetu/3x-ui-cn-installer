# Service assets

This directory keeps fallback service files used by the installer when upstream release archives do not include a matching service definition.

The automatic sync workflow follows the latest upstream scripts. These service assets are intentionally kept small and generic.
